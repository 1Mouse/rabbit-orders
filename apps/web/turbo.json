{
  "extends": ["//"],
  "tasks": {
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "env": ["NEXT_PUBLIC_APP_URL"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/types/**", ".next/dev/types/**"]
    }
  }
}
